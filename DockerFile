FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv && \
    uv pip install --system --no-cache-dir \
    mcp beautifulsoup4 requests

COPY servers/search_server.py .

CMD ["python", "search_server.py"]